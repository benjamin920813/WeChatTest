var mongo = require('shoots-mongo');
var assert = require("assert");
var mocha = require('mocha');
var coMocha = require('co-mocha');
var co = require("co");
var phantom = require('phantom');

coMocha(mocha);

/*
var app = {
    id: 'sh55b3342897d26',
    secret: 'd40fd6ee2db5d9ff3924462b0a399e55',
    name: 'app2',
    server: 'http://127.0.0.1:3000'
};
*/
var app = {
	id: 'sh5600bbe2cef5a',
	secret: '44c04a63390014ab497fed3f58ee5ab3',
	name: "test",
	server: 'http://dev.api.zpage.me'
};

var session = require('../index')(app);

phantom.create(function(ph) {

	ph.createPage(function(page) {
		page.open("http://127.0.0.1:3000/get", function(status) {
			console.log("", status);
			page.evaluate(function() {
				return document;
			}, function(result) {
				console.log('Page ' + result);
				ph.exit();
			});
		});
	});
});


describe('test session set', function() {
	it('it should return true', function*() {


	});
});

describe('test session get', function() {
	it('it should return true', function*() {

	});
});

describe('test session remove', function() {
	it('it should return the true', function*() {

	});
});

describe('test session clear', function() {
	it('it should return the true', function*() {
		//var val = yield session.clear();
		//assert.equal(true, val);
	});
});